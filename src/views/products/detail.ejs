<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de producto</title>
</head>
<body>
    <h1> Detalle de producto</h1>
   <% if (product.id) { %>
    <span>
        <%= product.name%> 
    </span> Precio $
    <%= product.price%> 
    <span>
        offert:<%= product.offert%>

    <% } %>
    <form action="/products/detail/?m=DELETE" method="post">
        <button type="submit">
         <i class="fas fa-trash tacho"></i>
        </button>
    </form>
    <a href="/products/update/<%= product.id %>">Modificar producto</a>
    
</body>
</html>